{
  "name": "sydney-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/changelog.mjs",
    "dev": "vite",
    "prebuild": "node ../scripts/changelog.mjs",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "@webgpu/types": "^0.1.71",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
